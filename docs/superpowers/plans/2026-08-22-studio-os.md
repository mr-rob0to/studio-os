# Studio OS Delivery Plan

**Done:** Tasks 1 through 4 are merged. Task 5 implementation, review, and CI gates are complete.
**Next:** Merge pull request #5, then start Task 6 in a fresh session on `feat/studio-06-brief-detail-analysis`.
**Blocked:** None.

**Date:** 2026-08-22
**Repository:** `mr-rob0to/studio-os`
**Status:** Task 5 complete; pull request #5 is ready for merge

## Product scope

Studio OS is an internal animation-studio brief application. A teammate submits a brief, the system persists it, a configured model provider produces validated decision-oriented analysis, and the interface presents the result or a recoverable failure state.

The release includes brief submission, persistence, list and detail views, structured analysis, retry, PGlite locally and in tests, Neon in preview and production, a database-aware health endpoint, Vercel deployment, and ownership documentation.

Authentication, editing, deletion, collaboration, uploads, queues, model routing, external monitoring, and additional product capabilities remain outside this plan.

## Technical constraints

- Use the Next.js App Router with strict TypeScript and plain CSS.
- Keep pages and layouts as Server Components unless browser interaction requires a client boundary.
- Use Zod for input, output, and API contracts.
- Use one Drizzle PostgreSQL schema and committed migrations for PGlite and Neon.
- Keep application database composition platform-neutral: runtime modules must not read Vercel or another hosting provider's environment variables.
- Require explicit `APP_ENV=local|test|preview|production` and `DATABASE_DRIVER=pglite|neon` configuration; do not default a missing database driver to PGlite.
- Allow PGlite only for `local` and `test`; reject it for `preview` and `production`, and require a server-only `DATABASE_URL` whenever the Neon driver is selected.
- Treat provider output as `unknown`; validate it centrally before persistence or rendering.
- Persist the brief and pending analysis before provider execution.
- Retry must claim an analysis atomically and recover stale pending work.
- Builds must not connect to a database or model provider.
- Pull request CI must prove committed migrations apply to clean PGlite and reject Drizzle schema changes without matching committed migration history.
- A successful merge to `main` must trigger the CI-owned production release: run the full quality gate, migrate Neon when schema or migration files changed, then deploy production through the Vercel CLI.
- Production deployment must not start when migration fails. Production migrations must remain backward-compatible because a later deployment failure can leave the database ahead of the application.
- Native Vercel production deployment from `main` must be disabled to prevent duplicate or out-of-order releases; Vercel Git preview deployments remain enabled for pull requests.
- Every non-docs task receives exactly one fresh read-only Claude Code Fable review after its pull request is open and the branch is finished.
- Every task uses a standard short-lived feature branch; use a worktree only when genuinely parallel work requires more than one active checkout.
- New capabilities, external services, or more than 30 minutes of unplanned work require a scope checkpoint.

## Live delivery tasks

### Task 1: Project standards and executable foundation

- [x] Branch is `feat/studio-01-foundation` in an isolated worktree based on fresh `origin/main`.
- [x] `.worktrees/` is locally excluded and the tracked `.gitignore` is the first standalone commit.
- [x] Current stable Next.js App Router foundation uses strict TypeScript, ESLint, Vitest, and pnpm.
- [x] Minimal accessible shell runs without database or provider access during build.
- [x] `.env.example`, GitHub Actions CI, README setup, constitution v1.0.0, and `AGENTS.md` are present.
- [x] Clean install, lint, typecheck, tests, production build, and local application start pass.
- [x] Open PR receives one fresh read-only Claude Code Fable review; required findings are resolved.
- [x] Branch is rebased, clean, pushed, and open as a review-ready pull request into `main`.

**Definition of done:** The foundation is reproducible from README commands, constitution v1.0.0 is active, CI passes, the pull request explains the chosen foundation, and Task 2 has not begun.

### Task 2: Shared contracts and PostgreSQL persistence

- [x] Add Zod contracts, Drizzle PostgreSQL schema, PGlite and Neon adapters, repository, migration runner, initial migration, test harness, and environment documentation.
- [x] Prove local PGlite persistence, isolated in-memory tests, Neon production composition, and one migration history for both drivers.
- [x] Make CI apply the committed migration to clean PGlite without production credentials; keep automatic production Neon migration in Task 9.
- [x] Keep `content_type` as unconstrained PostgreSQL text while Zod enforces current product values.

**Dependency:** Task 1 pull request merged and the Neon connection supplied securely before Neon verification.

### Task 3: Structured analysis providers

- [x] Add the provider interface, prompt, deterministic mock, OpenAI adapter, analysis service, environment validation, and focused tests.
- [x] Prove success, timeout, refusal or provider failure, malformed output, local Zod parsing, and no silent fallback.

**Dependency:** Task 2 pull request merged.

### Task 4: Brief workflow and mutation endpoints

- [x] Add the brief service, safe HTTP mapping, request IDs, create route, retry route, and workflow tests.
- [x] Replace the temporary Vercel-specific database guard with platform-neutral, Zod-validated application configuration; require explicit `APP_ENV` and `DATABASE_DRIVER`, reject PGlite for preview and production, and keep provider-specific environment mapping outside runtime database modules.
- [x] Update `.env.example`, local setup documentation, and focused configuration tests for missing variables, invalid environment-driver combinations, Neon credentials, and proof that hosted paths cannot import or create PGlite.
- [x] Prove validation, persistence before analysis, recorded failures, atomic retry, stale recovery, and consistent envelopes.

**Dependency:** Task 3 pull request merged.

### Task 5: Brief list and submission interface

- [x] Add dynamic brief list and new-brief pages, accessible form interaction, shared styles, and rendered validation tests.
- [x] Prove empty and populated states, value preservation, duplicate prevention, failure handling, and detail navigation.

**Dependency:** Task 4 pull request merged.

### Task 6: Brief detail, analysis, and retry interface

- [ ] Add the dynamic detail page, analysis panel, retry interaction, styles, and rendered tests.
- [ ] Prove completed, failed, pending, concurrent retry, stale recovery, and not-found states.

**Dependency:** Task 5 pull request merged.

### Task 7: Database health endpoint extension

- [ ] Add a bounded no-store health route, healthy and degraded tests, and endpoint documentation.
- [ ] Prove real database readiness, a two-second database budget, no model call, and no leaked internals.

**Dependency:** Task 6 pull request merged.

### Task 8: Architecture, README, and ownership documentation

- [ ] Complete setup, architecture, schema, endpoint, field-change, verification, tradeoff, exclusion, roadmap, disclosure, learning, and walkthrough documentation.
- [ ] Follow the README from a clean checkout and verify every diagram, path, and command.

**Dependency:** Task 7 pull request merged.

### Task 9: CI-owned production release and preview readiness

- [ ] Modify `.github/workflows/ci.yml`, `package.json`, `pnpm-lock.yaml`, `README.md`, `docs/ARCHITECTURE.md`, and this plan; add `vercel.json` only for the production Git-deployment override.
- [ ] Extend GitHub Actions so pull requests verify migration consistency and each successful merge to `main` runs one serialized production release without canceling an in-progress release.
- [ ] Detect changes to `src/server/db/schema.ts`, `drizzle.config.ts`, and `drizzle/**`; fail on schema drift, and run `pnpm db:migrate:neon` before deployment whenever those paths changed.
- [ ] Pin the Vercel CLI, disable native Vercel production deployment for `main`, retain pull-request previews, and deploy production from CI only after quality and migration jobs succeed.
- [ ] Use a branch-restricted GitHub `production` environment. Store `DATABASE_URL` and `VERCEL_TOKEN` as environment secrets, store Vercel project and organization IDs as environment variables, and keep runtime secrets in Vercel.
- [ ] Configure Vercel preview and CI-owned production with the application-level `APP_ENV` and `DATABASE_DRIVER` variables; keep all translation from Vercel deployment state to those variables in workflow or platform configuration, not application database code.
- [ ] Prove a non-schema merge skips Neon migration and deploys, a schema merge migrates before deploying, and a migration failure blocks deployment. Record workflow and deployment URLs without exposing secrets.
- [ ] Run the full quality and security gates, smoke test the pull-request preview, and resolve all Critical and High security findings before merge.

**Definition of done:** The Task 9 merge to `main` automatically passes quality gates, applies pending committed Neon migrations before deployment when database paths changed, and produces exactly one Vercel production deployment. A failed migration produces no deployment.

**Dependency:** Task 8 pull request merged; the Vercel project is connected; native `main` production auto-deploy is ready to be disabled; and the GitHub `production` environment, Neon connection, Vercel token, project ID, and organization ID are configured before merge.

### Task 10: Production verification and final handoff

- [ ] Verify the successful Task 9 `main` release workflow, production create, persistence, refresh, retry failure, health, secret handling, and stable URL.
- [ ] Record the CI run, Vercel deployment, live result, rollback boundary, and completed walkthrough and delivery status.

**Dependency:** Task 9 pull request merged and production deployment complete.

## CI-owned release acceptance

- Pull request CI fails when Drizzle schema changes are not represented by committed migration history.
- A merge to `main` always passes the full quality gate before production release work begins.
- A merge that changes `src/server/db/schema.ts`, `drizzle.config.ts`, or `drizzle/**` applies pending committed migrations to Neon before Vercel deployment.
- A merge without database-path changes skips the Neon migration and still deploys production.
- Production releases are serialized and never canceled after migration begins.
- Migration failure blocks deployment; deployment failure after migration is recovered with a compatible forward fix or application rollback, never a destructive automatic database rollback.
- Native Vercel production deployment from `main` is disabled, so each merge creates exactly one CI-owned production deployment while pull requests retain preview deployments.
- Application database code reads only platform-neutral `APP_ENV`, `DATABASE_DRIVER`, and server-only database settings; Vercel-specific environment handling remains confined to deployment configuration.

## Task definition of done

- Only approved task files and necessary plan state changed.
- Task acceptance criteria and relevant lint, typecheck, tests, build, and runtime paths pass.
- Control-flow edits are reread in their full enclosing functions.
- The implementer inspects the completed diff.
- One fresh read-only Claude Code Fable review covers the finished non-docs branch after its pull request is open.
- Critical and Important findings are verified and resolved; Minor findings are logged.
- The branch is rebased on fresh `origin/main`, clean, and open as a review-ready pull request.
- The task status and checkbox are current, and the session stops before the next task.

## Pull request format

Every pull request contains short sections named What, Why, Definition of done, Verified, Tradeoff, and Next.
